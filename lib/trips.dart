// HomeScreen.dart
import 'package:flutter/material.dart';

class Trips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trips Screen'),
      ),
      body: Center(
        child: Text('Trips Screen Content'),
      ),
    );
  }
}
